# 修复总结：本地环境一键启动+自检

## 修改的文件列表

### 1. scripts/dev.ps1
**目的**：提供一键启动+自检功能，自动检测端口占用、启动服务、验证服务

**改动点**：
- ✅ 端口占用检查：使用 `Get-NetTCPConnection` 获取真实 PID，打印可复制的 `taskkill` 命令（PID 是真实数字，不是 `<PID>` 占位符）
- ✅ 目录验证：启动前端前验证 `app/frontend` 目录和 `package.json` 存在，避免 `ENOENT: Could not read package.json` 错误
- ✅ 自动验证：启动后自动 curl 检查后端和前端是否正常
- ✅ 错误提示：失败时输出失败原因和查看日志的命令（明确指向窗口 A/B）

**验证方法**：
```powershell
# 执行脚本
.\scripts\dev.ps1

# 预期输出：
# 1. 端口检查：如果被占用，会打印真实 PID 和可复制的 taskkill 命令
# 2. 后端启动：看到 "✅ 后端启动成功 (耗时 Xs)"
# 3. 前端启动：看到 "✅ 前端启动成功 (耗时 Xs)"
# 4. 自动验证：看到 "✅ 后端健康检查通过" 和 "✅ 前端响应正常"
# 5. 访问链接：打印所有访问链接
```

### 2. README.md
**目的**：明确三窗口分工，避免用户在错误目录执行命令

**改动点**：
- ✅ 新增"本地运行（最少步骤）"章节：用表格清晰说明三窗口分工
- ✅ 明确窗口 B 必须在 `app/frontend` 目录执行 `npm run dev`，避免 `ENOENT` 错误
- ✅ 添加"验证清单"章节：3 条验证命令，每条都有预期输出
- ✅ 更新"常见问题排查"：端口占用时使用真实 PID（不是 `<PID>` 占位符）

**验证方法**：
```powershell
# 阅读 README.md 的"本地运行（最少步骤）"章节
# 预期：看到清晰的表格说明三窗口分工
# 预期：看到"窗口 B 必须在 app/frontend 目录执行 npm run dev"的明确提示
```

### 3. app/frontend/package.json
**目的**：确保端口固定为 3010，不允许 Next.js 自动换端口

**改动点**：
- ✅ 已固定：`"dev": "next dev -H 127.0.0.1 -p 3010"`
- ✅ 不需要修改：端口已正确固定

**验证方法**：
```powershell
# 检查 package.json
Get-Content app/frontend/package.json | Select-String "dev"

# 预期输出：
#     "dev": "next dev -H 127.0.0.1 -p 3010",
```

## 验证清单（3条命令）

在窗口 C（验证窗口）执行以下命令：

### 1. 检查前端端口监听
```powershell
netstat -ano | findstr ":3010"
```
**预期输出**：
```
TCP    127.0.0.1:3010    0.0.0.0:0    LISTENING    12345
```
**判断规则**：
- ✅ 看到 `LISTENING` 状态 → 前端正在监听
- ✅ `Local Address` 为 `127.0.0.1:3010` → 端口正确
- ❌ 无输出 → 前端未启动，检查窗口 B

### 2. 检查后端健康
```powershell
curl http://127.0.0.1:8000/health
```
**预期输出**：
```json
{"status":"ok"}
```
**判断规则**：
- ✅ 返回 `{"status":"ok"}` → 后端正常
- ❌ 连接被拒绝 → 后端未启动，检查窗口 A
- ❌ 返回其他内容 → 检查后端日志

### 3. 检查前端响应
```powershell
curl -I http://127.0.0.1:3010/
```
**预期输出**：
```
HTTP/1.1 200 OK
```
**判断规则**：
- ✅ 返回 `HTTP/1.1 200 OK` → 前端正常
- ❌ 连接被拒绝 → 前端未启动，检查窗口 B
- ❌ 返回其他状态码 → 检查前端日志

## 完整验证流程

### 步骤 1：启动服务

**方式一（推荐）**：使用一键脚本
```powershell
.\scripts\dev.ps1
```

**方式二**：手动启动
- 窗口 A：`python -m uvicorn app.api.main:app --host 127.0.0.1 --port 8000 --log-level info`
- 窗口 B：`cd app/frontend && npm run dev`

### 步骤 2：执行验证清单（窗口 C）

```powershell
# 1. 检查前端端口
netstat -ano | findstr ":3010"
# 预期：看到 LISTENING 状态

# 2. 检查后端健康
curl http://127.0.0.1:8000/health
# 预期：{"status":"ok"}

# 3. 检查前端响应
curl -I http://127.0.0.1:3010/
# 预期：HTTP/1.1 200 OK
```

### 步骤 3：浏览器访问

打开浏览器访问：
- http://127.0.0.1:3010/merchant
- http://127.0.0.1:3010/taoke

**预期**：
- ✅ 页面在 30s 内加载完成（显示数据或错误提示）
- ✅ 不会无限 loading
- ✅ 不会出现 ERR_CONNECTION_REFUSED

## 关键修复点

1. ✅ **端口占用检查**：使用真实 PID，打印可复制的 `taskkill` 命令（不是 `<PID>` 占位符）
2. ✅ **目录验证**：启动前端前验证 `app/frontend` 目录和 `package.json` 存在
3. ✅ **自动验证**：启动后自动 curl 检查后端和前端
4. ✅ **错误提示**：失败时输出失败原因和查看日志的命令（明确指向窗口 A/B）
5. ✅ **文档完善**：README 中明确三窗口分工和验证清单

## 如果验证失败

### 验证 1 失败（端口未监听）
- **检查窗口 B**：查看 `npm run dev` 输出
- **常见原因**：未在 `app/frontend` 目录执行，或端口被占用
- **解决方法**：确保在 `app/frontend` 目录，或释放端口

### 验证 2 失败（后端无响应）
- **检查窗口 A**：查看后端启动日志
- **常见原因**：Python 环境问题、依赖缺失、端口被占用
- **解决方法**：检查 Python 环境，安装依赖，释放端口

### 验证 3 失败（前端无响应）
- **检查窗口 B**：查看前端启动日志
- **常见原因**：npm 依赖缺失、编译错误、端口被占用
- **解决方法**：`npm install`，修复编译错误，释放端口

